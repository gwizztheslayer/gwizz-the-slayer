import arsenal from './schemas/arsenal';
import track from './schemas/track';
import comic from './schemas/comic';
import service from './schemas/service';

export const schema = {
  types: [arsenal, track, comic, service],
};
